# gLabels Batch Service
# Copy this file to .env and adjust values as needed

# ======================
# Environment mode
# ======================
# Environment: development / production
# Controls safety validations and logging defaults
ENVIRONMENT=development

# ======================
# Server settings
# ======================
HOST=0.0.0.0
PORT=8000

# Enable auto-reload (useful in development)
# Development: true (hot reload on file changes)
# Production: MUST be false (CRITICAL: will cause startup error if true in production!)
RELOAD=true

# ======================
# Application settings
# ======================

# Keep intermediate CSV files for debugging
# Development: true (useful for debugging)
# Production: false (saves disk space)
KEEP_CSV=true

# Maximum number of parallel jobs
# 0 = auto (CPU count - 1), >0 = explicit number (e.g. 4)
MAX_PARALLEL=0

# Maximum labels per batch before auto-split and PDF merge
# 0 = no limit, >0 = split large jobs into batches
MAX_LABELS_PER_BATCH=300

# Maximum labels per job request
MAX_LABELS_PER_JOB=2000

# Timeout per job in seconds (default 600 = 10 minutes)
GLABELS_TIMEOUT=600

# Hours to retain job state in memory before cleanup
RETENTION_HOURS=24

# Maximum request body size in bytes (default 5MB)
MAX_REQUEST_BYTES=5000000

# Maximum number of fields per label record
MAX_FIELDS_PER_LABEL=50

# Maximum length per field value
MAX_FIELD_LENGTH=2048

# ======================
# Logging settings
# ======================#

# Log level: DEBUG / INFO / WARNING / ERROR
# Development: DEBUG (verbose output)
# Production: INFO or WARNING (less verbose)
LOG_LEVEL=DEBUG

# Directory for log files (relative or absolute). Default: logs
LOG_DIR=logs

# ======================
# CORS settings
# ======================

# Comma-separated list of allowed origins. Leave empty to disable CORS.
# Example: CORS_ALLOW_ORIGINS=http://localhost:3000,https://example.com
CORS_ALLOW_ORIGINS=
