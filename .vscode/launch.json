{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI: gLabels Batch (Dev)",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "app.main:app",
        "--reload",
        "--host",
        "0.0.0.0",
        "--port",
        "8000"
      ],
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}",
      "jinja": false,
      "justMyCode": true,
      "console": "integratedTerminal"
    }
  ]
}
